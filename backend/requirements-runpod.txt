# Subtide Backend - RunPod/NVIDIA GPU Requirements
# For CUDA-enabled GPUs (RTX 4090, A100, etc.)

-r requirements.txt

# Whisper - faster-whisper with CTranslate2 for CUDA
faster-whisper

# Speaker diarization - NeMo for fast GPU-native processing
nemo_toolkit[asr]

# Fallback diarization (if NeMo fails)
pyannote.audio

# PyTorch with CUDA 12.1 support
# Note: Install separately with: pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
# torch
# torchaudio

# RunPod serverless SDK
runpod

# ONNX Runtime with GPU support
onnxruntime-gpu
