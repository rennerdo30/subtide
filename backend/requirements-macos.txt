# Subtide Backend - macOS Requirements
# For Apple Silicon (M1/M2/M3) with Metal GPU acceleration

-r requirements.txt

# Whisper - MLX for Metal GPU acceleration
mlx-whisper

# Speaker diarization (4.0.3+ required - uses in-memory waveform to bypass torchcodec)
pyannote.audio>=4.0.3

# PyTorch (CPU/MPS)
torch
torchaudio

# Fallback whisper (for compatibility)
openai-whisper

# ONNX Runtime for silero-vad
onnxruntime
